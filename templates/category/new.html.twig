{% extends 'base.html.twig' %}

{% block title %}Wild Series
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mt-5 mb-5 text-center">Add program category</h1>
        <div class="form-control text-center">
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" class="btn btn-danger mt-4 mb-2">Add category</button>
		{{ form_end(form) }}
        </div>
	</div>
{% endblock %}
